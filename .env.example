# 複製成 .env.local 並填入你的 Supabase 專案的值。
# Dashboard → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# 舊專案的 dashboard 上叫 anon key，新專案叫 publishable key（sb_publishable_...）。
# 兩個名字都讀得到，填其中一個就好。這把 key 本來就會出現在瀏覽器端，不是機密。
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# ── 以下是選填 ──────────────────────────────────────────────

# 只有名單內的 email 收得到登入連結（逗號分隔）。不設就是任何人都能自行註冊帳號。
# 這是單人使用時的第一道鎖，第二道請到 Supabase Dashboard 關掉 sign up。
# ALLOWED_EMAILS=you@example.com

# 登入連結要回跳的網址。不設會從 request header 推導，本機開發不用管；
# 部署到 Vercel 之後填正式網址比較保險。
# NEXT_PUBLIC_SITE_URL=https://notes.example.com

# ── 垃圾桶的定期清理（/api/cron/purge）─────────────────────
# 兩個都設了才會運作，缺一個那個端點會回 503 並說明缺什麼。
# 只在伺服器端使用，沒有 NEXT_PUBLIC_ 前綴就不會進到瀏覽器。

# Dashboard → Project Settings → API → service_role key。
# 這把 key 等於資料庫的最高權限，只放在 Vercel 的環境變數裡，不要進版控。
# SUPABASE_SERVICE_ROLE_KEY=

# 排程端點的通行碼，隨便一串長亂碼即可（例如 openssl rand -hex 32）。
# 在 Vercel 設定同名變數之後，它的 Cron 會自動帶 Authorization: Bearer 這個值。
# CRON_SECRET=
