# 複製成 .env.local 並填入你的 Supabase 專案的值。
# Dashboard → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# 舊專案的 dashboard 上叫 anon key，新專案叫 publishable key（sb_publishable_...）。
# 兩個名字都讀得到，填其中一個就好。這把 key 本來就會出現在瀏覽器端，不是機密。
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
